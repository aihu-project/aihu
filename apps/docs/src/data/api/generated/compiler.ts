// GENERATED by scripts/gen-api.ts — do not edit. Source: node_modules/@aihu/compiler
import type { ApiExport, ApiPackage } from '../types.ts'

export const PKG: ApiPackage = {
  name: '@aihu/compiler',
  slug: 'compiler',
  tier: 'Compiler & tooling',
  version: '1.3.5',
  tagline: 'Single File Component (.aihu) compiler — Rust binary + JS glue.',
  note: '',
}

export const EXPORTS: readonly ApiExport[] = [
  {
    name: '_buildDeferredHydration',
    kind: 'function',
    signature: 'function xe(e,t)',
    summary: '',
  },
  {
    name: '_buildStaticIsland',
    kind: 'function',
    signature: 'function Se(e,t)',
    summary: '',
  },
  {
    name: '_clearTransformMemo',
    kind: 'function',
    signature: 'function ce()',
    summary: '',
  },
  {
    name: '_compileViaBackend',
    kind: 'function',
    signature: 'function j(e,n,r)',
    summary: '',
  },
  {
    name: '_deriveChildTags',
    kind: 'function',
    signature: 'function Y(e)',
    summary: '',
  },
  {
    name: '_errMessage',
    kind: 'function',
    signature: 'function W(e)',
    summary: '',
  },
  {
    name: '_foldCssEngineStyles',
    kind: 'function',
    signature: 'function X(e,t)',
    summary: '',
  },
  {
    name: '_foldCssEngineStylesGlobal',
    kind: 'function',
    signature: 'function je(e,t,n)',
    summary: '',
  },
  {
    name: '_foldSsrCssExport',
    kind: 'function',
    signature: 'function Ee(e,t)',
    summary: '',
  },
  {
    name: '_formatExtractCensus',
    kind: 'function',
    signature: 'function fe(e)',
    summary: '',
  },
  {
    name: '_getCompilerNativeStateKind',
    kind: 'function',
    signature: 'function x()',
    summary: '',
  },
  {
    name: '_globalizeAuthoredStyle',
    kind: 'function',
    signature: 'function H(e)',
    summary: '',
  },
  {
    name: '_hashIdForUtilityCss',
    kind: 'function',
    signature: 'function ke(e)',
    summary: '',
  },
  {
    name: '_injectAutoWiring',
    kind: 'function',
    signature: 'function Z(e)',
    summary: '',
  },
  {
    name: '_injectLightScopeId',
    kind: 'function',
    signature: 'function V(e,t)',
    summary: '',
  },
  {
    name: '_injectShadowMode',
    kind: 'function',
    signature: 'function B(e,t,n)',
    summary: '',
  },
  {
    name: '_isLayoutFile',
    kind: 'function',
    signature: 'function ge(e,t)',
    summary: '',
  },
  {
    name: '_isViteMissing',
    kind: 'function',
    signature: 'function G(e)',
    summary: '',
  },
  {
    name: '_layoutTag',
    kind: 'function',
    signature: 'function _e(e)',
    summary: '',
  },
  {
    name: '_lightScopeId',
    kind: 'function',
    signature: 'function Ae(e)',
    summary: '',
  },
  {
    name: '_parseComponentTagsMarker',
    kind: 'function',
    signature: 'function J(e)',
    summary: '',
  },
  {
    name: '_parseExtractMarker',
    kind: 'function',
    signature: 'function de(e)',
    summary: '',
  },
  {
    name: '_parseIslandMarker',
    kind: 'function',
    signature: 'function U(e)',
    summary: '',
  },
  {
    name: '_passivizeOutlet',
    kind: 'function',
    signature: 'function ye(e)',
    summary: '',
  },
  {
    name: '_resetCompileBackend',
    kind: 'function',
    signature: 'function ie()',
    summary: '',
  },
  {
    name: '_resetCompilerNative',
    kind: 'function',
    signature: 'function S()',
    summary: '',
  },
  {
    name: '_resolveCompileBackend',
    kind: 'function',
    signature: 'function O()',
    summary: '',
  },
  {
    name: '_stripFailure',
    kind: 'function',
    signature: 'function K(e,t,n,r,i)',
    summary: '',
  },
  {
    name: '_stripTypes',
    kind: 'function',
    signature: 'async function q(e,t,n,r)',
    summary: '',
  },
  {
    name: '_transformMemoStats',
    kind: 'function',
    signature: 'function le()',
    summary: '',
  },
  {
    name: 'aihuCompilerPlugin',
    kind: 'function',
    signature: 'function Re(e)',
    summary: '',
  },
  {
    name: 'compileRouteMeta',
    kind: 'function',
    signature: 'function Pe(e,t)',
    summary: '',
  },
  {
    name: 'compileSidecar',
    kind: 'function',
    signature: 'function Me(e,n,i)',
    summary: '',
  },
  {
    name: 'compileToAst',
    kind: 'function',
    signature: 'function Ne(e,t)',
    summary: '',
  },
  {
    name: 'kebabComponentTag',
    kind: 'function',
    signature: 'function ve(e)',
    summary: '',
  },
  {
    name: 'loadCompilerNative',
    kind: 'function',
    signature: 'function b()',
    summary: '',
  },
  {
    name: 'matchBrace',
    kind: 'function',
    signature: 'function t(t,n)',
    summary: '',
  },
  {
    name: 'matchParen',
    kind: 'function',
    signature: 'function n(n,r)',
    summary: '',
  },
  {
    name: 'migrate',
    kind: 'function',
    signature: 'function e(e)',
    summary: '',
  },
  {
    name: 'migrateStateWrappers',
    kind: 'function',
    signature: 'function E(e,t={})',
    summary: '',
  },
  {
    name: 'migrateTemplateGrammar',
    kind: 'function',
    signature: 'function s(e)',
    summary: '',
  },
  {
    name: 'transform',
    kind: 'function',
    signature: 'function Ce(e,n,i)',
    summary: '',
  },
  {
    name: '_MEMO_MAX_ENTRIES',
    kind: 'const',
    signature: 'const oe',
    summary: '',
  },
  {
    name: 'resolveCompilerBinary',
    kind: 'const',
    signature: 'const resolveCompilerBinary',
    summary: '',
  },
  {
    name: 'VIRTUAL_UTILITY_PREFIX',
    kind: 'const',
    signature: 'const De',
    summary: '',
  },
  {
    name: 'AihuCompilerPluginOptions',
    kind: 'interface',
    signature:
      "interface AihuCompilerPluginOptions {\n  /**\n   * When `true` (default), components the compiler classified as `'static'`\n   * (read from the `// @aihu:island` marker via `_parseIslandMarker()`) are\n   * emitted with a minimal HTML-only registration shim that ships **zero**\n   * `@aihu/runtime` and `@aihu/signals` JS to the browser. Components\n   * classified as `'interactive'` retain the full runtime path.\n   *\n   * Setting `islands: false` opts every component back into the unified\n   * runtime path (Plan 3.2 baseline behaviour).\n   */\n  islands?: boolean;\n  /**\n   * Project-wide rendering mode applied to every `.aihu` SFC compiled\n   * by this plugin instance. When set, the plugin post-processes the\n   * compiled JS to inject `, { shadowMode: '<mode>' }` as the third arg\n   * to the emitted `defineElement(tag, defineComponent(...))` call.\n   *\n   * BINARY vocabulary (DA4 #437):\n   * - `'shadow'` — shadow DOM (`attachShadow({ mode: 'open' })` internally;\n   *               open is the only browser mode aihu's composition/hydration\n   *               can use). `this.shadowRoot` is the non-null root.\n   * - `'light'`  — **no shadow root.** The component mounts into its own\n   *               element (`this.shadowRoot === null`). Required for global\n   *               utility-class CSS frameworks like Tailwind, UnoCSS, Pico\n   *               that rely on the cascade.\n   *\n   * Per-file override: the `$shadow: 'light' | 'shadow'` macro outranks this\n   * config. Unset, pages/layouts default to `'light'` and leaves to\n   * `'shadow'`.\n   */\n  shadowMode?: 'light' | 'shadow';\n  /**\n   * Build target threaded to the compiler binary (`--target`). Defaults to the\n   * compiler's `universal` target (current behaviour). Set to `'client'` for a\n   * browser bundle that must NOT ship the server `__agentBinding` (policy) and\n   * instead gets the policy-free `@agent` opaque-ID dispatcher + the per-instance\n   * `_registerAgentDispatcher` wiring the capability bridge reads after mount.\n   * See `examples/agent-driven-demo`.\n   */\n  target?: 'client' | 'server' | 'universal';\n  /**\n   * Directory (relative to the project root) holding layout SFCs. Default:\n   * `'src/layouts'`. Files under this directory are compiled in **layout mode**:\n   * their custom element is registered under the namespaced tag\n   * `aihu-layout-<stem>` (a layout stem like `app` is not a valid custom-element\n   * name on its own), and their `<outlet>` lowers to a **passive**\n   * `data-aihu-outlet` marker rather than the reactive route-driven boundary —\n   * because `@aihu/app`'s client renderer fills the marker imperatively and the\n   * reactive boundary would otherwise clear it on mount.\n   *\n   * Kept in sync with `@aihu/router`'s `layoutTagFor()` (`virtual:aihu-layouts`).\n   */\n  layoutsDir?: string;\n}",
    summary: 'Options for `aihuCompilerPlugin()` (Plan 3.3 — Islands).',
  },
  {
    name: 'CompileEnvelope',
    kind: 'interface',
    signature:
      'interface CompileEnvelope {\n  envelope: number;\n  targets: Record<string, {\n    js?: string;\n    manifest?: string;\n  }>;\n  astJson?: string;\n  routeJson?: string;\n  diagnostics: unknown[];\n}',
    summary: 'The wire shape of a compile envelope (Rust `Envelope`, camelCase).',
  },
  {
    name: 'CompileEnvelopeOptions',
    kind: 'interface',
    signature:
      "interface CompileEnvelopeOptions {\n  tag?: string;\n  path?: string;\n  targets?: string[];\n  emits?: Array<'js' | 'ast' | 'route' | 'manifest'>;\n  strictTemplates?: boolean;\n  exprParser?: string;\n}",
    summary: 'Options forwarded to the Rust envelope API (Rust `EnvelopeOptions`).',
  },
  {
    name: 'MigrateResult',
    kind: 'interface',
    signature:
      'interface MigrateResult {\n  readonly rewritten: string;\n  readonly warnings: readonly string[];\n}',
    summary:
      'macro-simplification codemod (B6.2) Transforms v1 `.aihu` source into v2 collection-form per `docs/superpowers/specs/2026-05-05-spec-macro-vocabulary-v2.md`.',
  },
  {
    name: 'RouteMeta',
    kind: 'interface',
    signature:
      "interface RouteMeta {\n  pattern?: string;\n  name?: string;\n  layout?: string;\n  middleware?: string[];\n  ssr?: boolean;\n  params?: string[];\n  head?: unknown;\n  /**\n   * GX Phase 1 fan-out (#437-GX): the resolved `extract` policy — always\n   * present in the binary's route-json output since 0.1.12 (the default is\n   * recorded, never implied by absence). Typed loose here: consumers\n   * normalize fail-closed (`deriveReadPolicy` in `@aihu/server`).\n   */\n  extract?: {\n    read?: unknown;\n    call?: unknown;\n  };\n  /**\n   * GX Phase 4 fan-out (#466): the `data:` governed-resource declaration\n   * (70-governed-data-access §2.1) — present only when the route declares one\n   * (0.1.14+). `type` keys the server registry's provider; `preview` lists\n   * the locked-state fields (omitted when none declared). Consumers: the\n   * server runtime's boot validation + generated loader, and the router Vite\n   * layer's C486 sibling-loader conflict check (§4.7).\n   */\n  data?: {\n    type?: string;\n    preview?: string[];\n  };\n}",
    summary: 'Structured `@route` metadata (the `.route.json` sidecar shape).',
  },
  {
    name: 'SfcAst',
    kind: 'interface',
    signature:
      "interface SfcAst {\n  /** Resolved custom-element tag name (meta.name → route.name → file stem). */\n  tag: string;\n  /** AST schema version — bumped on any breaking shape change (semver-tied). */\n  astVersion: 1;\n  /** The @style block, if the SFC declared one. */\n  style: SfcStyleBlock | null;\n  /** Parsed template tree. null when the SFC has no @template block. */\n  template: SfcNode[] | null;\n  /** SFC-level metadata. */\n  meta: SfcMeta;\n  /**\n   * The compiler-assigned light-DOM scope id for this component's `data-a`\n   * attribute, present only when it resolved to `shadowMode: 'light'`\n   * (light-DOM leaf flip, LDF §10 step 1). Absent (not just `undefined`, the\n   * key itself omitted on the wire) for shadow-mode components — additive,\n   * mirrors `aihu-css-core`'s `SfcAst.light_scope_id: Option<String>`.\n   */\n  lightScopeId?: string;\n}",
    summary: 'Top-level AST export — one per .aihu SFC.',
  },
  {
    name: 'SfcMeta',
    kind: 'interface',
    signature:
      'interface SfcMeta {\n  /** From @meta { name } / @route { name } / file stem — never null after resolution. */\n  name: string;\n}',
    summary: '',
  },
  {
    name: 'SfcStyleBlock',
    kind: 'interface',
    signature:
      "interface SfcStyleBlock {\n  /** Verbatim CSS body of the @style block (braces stripped, $global token removed). */\n  content: string;\n  /** 'scoped' (default) or 'global' (@style { $global ... }). */\n  scope: 'scoped' | 'global';\n}",
    summary: '',
  },
  {
    name: 'StateWrapperOptions',
    kind: 'interface',
    signature:
      'interface StateWrapperOptions {\n  /** Run only stage 1 ($-macros + bare typed decls). */\n  macrosOnly?: boolean;\n  /** Run only stage 2 (signal tuples + call sites). */\n  tuplesOnly?: boolean;\n  /**\n   * Binding names that must stay PLAIN even when the reactive heuristic says\n   * `state(…)` (e.g. a fixture guard depends on the plain-let lowering).\n   */\n  keepPlain?: readonly string[];\n}',
    summary: '',
  },
  {
    name: 'StateWrapperResult',
    kind: 'interface',
    signature:
      'interface StateWrapperResult {\n  readonly rewritten: string;\n  readonly warnings: readonly string[];\n  /** True when the file was left byte-untouched (nothing to do, or aborted). */\n  readonly skipped: boolean;\n  /** Which change classes were applied — drives the verification bucket. */\n  readonly changes: {\n    macros: boolean;\n    tuples: boolean;\n    bareTyped: boolean;\n  };\n  /** Tuple pairs migrated: authored setter name → state binding name. */\n  readonly renamedSetters: ReadonlyArray<{\n    getter: string;\n    setter: string;\n  }>;\n}',
    summary:
      "state-wrapper codemod (#487 §7 — waves 1 + 2, the app-corpus migration) Migrates an `.aihu` file's `@state` block from the retired forms to the ratified wrapper model (`docs/plans/state-model/40-spec.md`): Stage 1 — `$`-macros → wrappers / statement calls / directives (§2, §3): $prop: { x: { … } } → const|let x = prop<T>({ … }) $computed: { x: fn } → const x = derived(cfg?, fn) $action: { f: fn } → const f = action(cfg?, fn) $resource: { r: fn } → const r = resource(cfg?, fn) $controller: { c: {value} }→ const c = controller(cfg?, factory) $effect: fn / { name: fn } → effect(fn) / effect({ on: […] }, fn) $lifecycle: { mount, … } → onMount(fn), onDispose(fn), … $aria: { … } / $form: { … }→ aria({ … }) / form({ … }) $context: {provide,consume}→ provide('k', v) / const k = consume<T>('k') $route name → const name = route() $before/afterNavigate(fn) → beforeNavigate(fn) / afterNavigate(fn) $extends: X / $shadow: 'm' → base: X / shadow: 'm' bare typed decl `x: T = v` → let x = state(v) (reactive) | plain let/const (inert; §3 row 21 + W627) Stage 2 — signal tuples → `state` (§7 wave 2), INCLUDING call sites: const [x, setX] = signal(v) → let x = state(v) x() → x setX(v) → x = v (statement / handler) setX(p => expr) → x = expr[p→x] (updater desugar) Byte-identity contract: `$`-macro migrations lower onto the SAME IR, so a stage-1-only migration emits byte-identical JS (modulo the old parser's `strip_line_comments` artifact — authored `//` comments inside metadata-bag bodies were BLANKED in old emission and are preserved by the wrapper path).",
  },
  {
    name: 'StripTypesResult',
    kind: 'interface',
    signature:
      "interface StripTypesResult {\n  readonly code: string;\n  readonly map: null;\n  /** Rolldown-only hint; set ONLY on the last-resort no-transform branch. */\n  readonly moduleType?: 'ts';\n}",
    summary: "What the Vite plugin's `transform` hook hands back after the strip.",
  },
  {
    name: 'TemplateGrammarResult',
    kind: 'interface',
    signature:
      'interface TemplateGrammarResult {\n  readonly rewritten: string;\n  readonly changed: boolean;\n}',
    summary:
      'template-grammar-v2 codemod — migrate v1 template syntax to the prefix-less grammar (docs/plans/template-grammar/40-spec.md).',
  },
  {
    name: 'ViteStripApi',
    kind: 'interface',
    signature:
      'interface ViteStripApi {\n  readonly version?: string;\n  readonly transformWithOxc?: (code: string, id: string, ...rest: any[]) => Promise<{\n    code: string;\n  }>;\n  readonly transformWithEsbuild?: (code: string, id: string, ...rest: any[]) => Promise<{\n    code: string;\n  }>;\n}',
    summary: 'The subset of the Vite module `_stripTypes` uses — the seam tests fake.',
  },
  {
    name: 'SfcAttr',
    kind: 'type',
    signature:
      "type SfcAttr = {\n  kind: 'static';\n  name: string;\n  value: string;\n} | {\n  kind: 'binding';\n  name: string;\n  expr: string;\n} | {\n  kind: 'macro';\n  name: string;\n  value: SfcMacroValue;\n};",
    summary: 'Discriminated union mirroring Rust `Attr` — the three class-forms key on `kind`.',
  },
  {
    name: 'SfcMacroValue',
    kind: 'type',
    signature:
      "type SfcMacroValue = {\n  form: 'quoted';\n  value: string;\n} | {\n  form: 'curly';\n  expr: string;\n} | {\n  form: 'boolean';\n};",
    summary: '',
  },
  {
    name: 'SfcNode',
    kind: 'type',
    signature:
      "type SfcNode = {\n  kind: 'element';\n  tag: string;\n  attrs: SfcAttr[];\n  children: SfcNode[];\n} | {\n  kind: 'macroElement';\n  name: string;\n  attrs: SfcAttr[];\n  children: SfcNode[];\n} | {\n  kind: 'text';\n  value: string;\n} | {\n  kind: 'interpolation';\n  expr: string;\n} | {\n  kind: 'ifBlock';\n  branches: Array<{\n    cond: string;\n    body: SfcNode[];\n  }>;\n} | {\n  kind: 'eachBlock';\n  list: string;\n  item: string;\n  idx: string | null;\n  key: string | null;\n  body: SfcNode[];\n  emptyBody: SfcNode[] | null;\n} | {\n  kind: 'htmlBlock';\n  expr: string;\n};",
    summary: 'Discriminated union mirroring Rust `TemplateNode`.',
  },
]
