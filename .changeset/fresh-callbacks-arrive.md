---
'@aihu/arbor': patch
'@aihu/compiler': patch
'@aihu/magna': patch
---

Pass `onX` callback props to child components without treating them as DOM event listeners.
Make Magna's unpublished GraphQL parser an explicit opt-in so Yarn installs retain
the documented graceful fallback.
