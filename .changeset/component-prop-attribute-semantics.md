---
"@aihu/compiler": patch
---

Keep dynamic `aria-*` and `data-*` bindings on child components in the DOM attribute channel while preserving direct component properties for callbacks and regular props. Resolve an installed optional CSS engine from the Vite consumer when package-store realpaths hide it from the compiler package.
